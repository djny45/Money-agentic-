export const metadata = {
  title: "Money-Agentic",
  description: "CPA campaign intelligence and automation dashboard"
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}